{
  "name": "productivity",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm-run-all build:*",
    "build:web": "yarn workspace web build",
    "build:scheduler": "yarn workspace scheduler build && netlify-lambda build packages/scheduler/build"
  },
  "dependencies": {
    "netlify-lambda": "2.0.1",
    "npm-run-all": "^4.1.5"
  }
}
